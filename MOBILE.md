# Web → native mobile

`Post` type + moderation flow in `src/App.tsx`. For Expo: move types + `localStorage` persistence to **`src/lib/feed.ts`** and swap storage for **AsyncStorage** or a small **Supabase** backend (recommended before real school rollout).
